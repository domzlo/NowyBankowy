<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>pl.vavatech.java</groupId>
  <artifactId>example-oop-1</artifactId>
  <version>1</version>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>8</source>
          <target>8</target>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>

<!-- plik maven'owy, który nadaje właściwości naszemu nowemu projektowi (pom - Project Object Model)
Maven - narzędzie do budowania aplikacji, możemy podać w jego konfiguracji jakich bibliotek będziemy wykozystywać
i dzięki temu nie musimy sami ich szukać -->
<!--poza Maven: stary (archaiczny) Make, Ant - w nich się pisze wszystko ręcznie...
Maven pozwolił na ustrukturyzowanie działań, katalogów i konwencji tworzenia aplikacji
Gradle - połączenie najlepszych rzeczy z Ant i Maven (ale pisze się w języku Groovy)-->